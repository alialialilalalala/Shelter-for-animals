<UserControl x:Class="AnimalShelterAI.MyApplicationsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" 
                Background="White" 
                CornerRadius="8" 
                Margin="10"
                Padding="15"
                BorderBrush="{DynamicResource MaterialDesignDivider}" 
                BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="ClipboardTextOutline" 
                                        Width="30" 
                                        Height="30" 
                                        Foreground="{DynamicResource PrimaryHueMidBrush}"
                                        Margin="0,0,10,0"/>
                <TextBlock Text="Мои заявки на усыновление" 
                           FontSize="18" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Список заявок -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8" 
                Margin="10"
                Padding="10"
                BorderBrush="{DynamicResource MaterialDesignDivider}" 
                BorderThickness="1">
            <DataGrid x:Name="dgApplications" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeRows="False"
                      CanUserSortColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№" 
                                       Width="50" 
                                       Binding="{Binding ApplicationId}"
                                       IsReadOnly="True"/>
                    <DataGridTextColumn Header="Животное" 
                                       Width="120" 
                                       Binding="{Binding AnimalName}"/>
                    <DataGridTextColumn Header="Вид" 
                                       Width="80" 
                                       Binding="{Binding AnimalType}"/>
                    <DataGridTextColumn Header="Дата заявки" 
                                       Width="100" 
                                       Binding="{Binding ApplicationDate, StringFormat=dd.MM.yyyy}"/>
                    <DataGridTemplateColumn Header="Статус" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" 
                                        Padding="5,2" 
                                        Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}}">
                                    <TextBlock Text="{Binding StatusDisplay}" 
                                               HorizontalAlignment="Center" 
                                               Foreground="White"
                                               FontWeight="Bold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Дата решения" 
                                       Width="100" 
                                       Binding="{Binding DecisionDate, StringFormat=dd.MM.yyyy}"/>
                    <DataGridTextColumn Header="Менеджер" 
                                       Width="120" 
                                       Binding="{Binding ManagerName}"/>
                    <DataGridTextColumn Header="Примечания" 
                                       Width="200" 
                                       Binding="{Binding Notes}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>